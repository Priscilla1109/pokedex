spring:
  application:
    name: Pokedex
  datasource:
    url: 'jdbc:h2:mem:testdb'
    driverClassName: org.h2.Driver
    username: pokedexadm
    password: pokedexadm
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
server:
  port: '8082'

jdbi:
  datasource:
    default: